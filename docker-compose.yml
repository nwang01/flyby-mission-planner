services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: missionplanner
      POSTGRES_USER: missionplanner
      POSTGRES_PASSWORD: missionplanner
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U missionplanner"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build: ./backend
    environment:
      DB_URL: jdbc:postgresql://db:5432/missionplanner
      DB_USERNAME: missionplanner
      DB_PASSWORD: missionplanner
    ports:
      - "8080:8080"
    depends_on:
      db:
        condition: service_healthy

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend

volumes:
  pgdata:
